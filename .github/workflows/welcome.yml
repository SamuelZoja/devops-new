name: Welcome to DevOps

   on:
     push:
       branches:
         - main
     workflow_dispatch:

   jobs:
     greet:
       runs-on: ubuntu-latest
       steps:
         - name: Echo welcome message
           run: echo "Welcome to Devops"
